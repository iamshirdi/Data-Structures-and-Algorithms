#include <cs50.h>
#include <stdio.h>

// Todo a lot stopped middle since notrequired
void sort( int numbers[], int c);

int main(void)
{
    int numbers[] = {10,3,4,5,6,7};
    for (int i = 0; i < 7; i++)
    {
        printf("%i   ", numbers[i]);    
    }

    sort(numbers, 7);
    
    
}
void sort( int numbers[], int c)
{
    int array[c][c];
    size=c/2; // 7/2 stating
    multiplier=1*2
    for(int i=0; i<c; i++)
    {
        
    }
    
    return 
}